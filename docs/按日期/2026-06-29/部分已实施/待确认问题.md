# 译员系统 开工前待确认

> 文档状态：部分已实施｜日期：2026-06-29｜说明：多数业务口径已确认并落地，角色范围等少数事项仍待确认。

0625 会议已对齐。

已定：

1. 计费按月在线表格（非日报），译员自报产能；字段含年月、姓名、金额、支付方式、账号、支付状态、个人信息，收款信息另关联一张表。现有记录不到 1000 条。
2. 开票不直连税务局，译员本人开票；系统汇总成账单，人工或 agent 邮件回收发票、更新支付状态。半自动。
3. 导入约 800~1000 译员，散在聊天记录；先定模板，资源端补全，再统一导入，需要时间。
4. 费率按任务类型分别定价，LQE 与 LQA 不同；增"其他类型"记杂项一口价；项目一口价单列。
5. 不做汇率换算，人工核对。
6. 资源端全编辑权限，boss 只读。
7. 线上服务器部署。每译员唯一 ID，按月自动汇总。

仍待定：

- 系统是否扩到全角色（资源、项目、前端）——待上级。
- 账单给译员：先做自动发邮件加译员确认，外部登录暂缓。
- 产能利用率公式——待 PM。


## 0629 资源方会议（与 Raven）— 补充已定 + 修订

补 / 改 0625：

1. 产能利用率字段 → **删**（算着复杂、参考意义不大，实际档期直接问译员本人，无法据此派活）；产能模块保留「占用 + 可用率」。
2. 计费 → **按字**（词数单位 = 字；项目组习惯按字）。
3. 付款单按月自动汇总 → **不做**；改为 Raven 导出**日报全表**给刘家劭自行解析（先导前几个月数据测试）。
4. 真实译员数据导入 → **先导几十条测试**，Raven 负责资源端补全，等刘家劭给**最终模板**。
5. 译员信息汇总 → **Raven 用 agent（读聊天 skill）辅助 + 人工把关**再填模板（全自动批量抽取相关性低，不可行）。
6. 外部登入口 → **不做**，维持邮件回收发票（译员邮件回发票 → 统一读取上传）。

仍待小明哥：① 系统角色范围（是否扩到项目/前端）；② 会议最后两项（转写不清，待澄清）。
