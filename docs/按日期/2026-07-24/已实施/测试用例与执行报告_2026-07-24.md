# 规则与基础迁移测试用例与执行报告

> 文档状态：已执行 / 已验收｜日期：2026-07-24｜对应迁移：`20260724_0002`

测试对象：译员性别与主体类型字段、项目经历表、Alembic 升降级、旧译员数据兼容和现有业务回归  
自动化主入口：`backend/tests/test_migrations.py`  
测试环境：macOS、Python 3.14.6、SQLAlchemy 2.0.51、Alembic 1.18.5、SQLite 3.53.3  
隔离策略：自动测试使用临时数据库；开发库验证使用 `backend/app.db` 的临时副本，不改动原库。

## 一、验收结论

- `20260717_0001` 可升级到 `20260724_0002`，可安全降级并再次升级。
- `translators` 新增的 `gender/entity_type` 均允许空值，旧译员记录可继续读取。
- `translator_project_experiences` 包含 14 个计划字段、译员外键和 3 个查询索引。
- 本次迁移不会依据旧 `current_project/role` 自动创建项目经历。
- 开发库副本往返迁移后，29 条译员记录均保留，完整性检查均为 `ok`。
- `alembic check` 输出 `No new upgrade operations detected.`。
- 核心业务验收 `71/71` 通过；其余后端、前端、安全和静态检查均通过。

## 二、前置条件

| 编号 | 前置条件 | 说明 |
|---|---|---|
| P-01 | Python 虚拟环境可用 | `backend/.venv/bin/python` |
| P-02 | 项目依赖已安装 | 使用 `backend/requirements.txt` |
| P-03 | 初始迁移存在 | `20260717_0001` |
| P-04 | 待测迁移存在 | `20260724_0002` |
| P-05 | 数据库测试隔离 | 使用临时目录或开发库副本 |
| P-06 | 降级前保留原数据 | 测试记录包含旧 `current_project/role` |

## 三、结构与空库迁移用例

| 编号 | 场景与步骤 | 预期结果 | 自动化映射 | 实测 |
|---|---|---|---|---|
| MIG-01 | 对空 SQLite 执行 `alembic upgrade head` | 命令成功，版本到 `20260724_0002` | `check_empty_database_upgrade` | 通过 |
| MIG-02 | 检查升级后的表集 | 13 张业务表及 `alembic_version` 齐全 | `check_empty_database_upgrade` | 通过 |
| MIG-03 | 检查 `translators.gender` | 字段存在且允许 `NULL` | `assert_profile_schema` | 通过 |
| MIG-04 | 检查 `translators.entity_type` | 字段存在且允许 `NULL` | `assert_profile_schema` | 通过 |
| MIG-05 | 检查项目经历字段集合 | 14 个计划字段齐全，无意外字段 | `assert_profile_schema` | 通过 |
| MIG-06 | 检查项目经历必填字段 | `translator_id/cooperation_source/project_status/project_name` 不允许空 | `assert_profile_schema` | 通过 |
| MIG-07 | 检查译员外键 | `translator_id` 指向 `translators.id` | `assert_profile_schema` | 通过 |
| MIG-08 | 检查项目经历索引 | 译员、合作来源、项目状态索引均存在 | `assert_profile_schema` | 通过 |
| MIG-09 | 检查空库业务数据 | 译员和项目经历均为 0 条 | `check_empty_database_upgrade` | 通过 |
| MIG-10 | 从 head 降级到 `base` | 业务表删除，仅保留空版本表 | `check_empty_database_upgrade` | 通过 |
| MIG-11 | 降级后再次升级到 head | 完整结构可重新建立 | `check_empty_database_upgrade` | 通过 |
| MIG-12 | 仅导入 `app.main` | 不自动创建或修改数据库表 | `check_application_import_does_not_create_schema` | 通过 |

## 四、旧版本升级与数据兼容用例

测试初始状态：数据库版本为 `20260717_0001`，预置译员 ID 7，姓名“保留数据”，旧项目“旧当前项目”，角色“翻译”。

| 编号 | 场景与步骤 | 预期结果 | 自动化映射 | 实测 |
|---|---|---|---|---|
| COMP-01 | 从 `20260717_0001` 升级到 head | 迁移成功，版本为 `20260724_0002` | `check_initial_revision_upgrade_round_trip` | 通过 |
| COMP-02 | 读取旧译员新增字段 | `gender/entity_type` 均为 `NULL` | 同上 | 通过 |
| COMP-03 | 使用 SQLAlchemy ORM 读取旧译员 | 记录存在且可正常读取 | 同上 | 通过 |
| COMP-04 | 调用旧译员 `as_dict()` | 旧 `current_project` 仍为“旧当前项目” | 同上 | 通过 |
| COMP-05 | 检查项目经历记录数 | 为 0，不自动迁移旧项目字段 | 同上 | 通过 |
| COMP-06 | 升级后执行完整性检查 | `PRAGMA integrity_check` 返回 `ok` | 同上 | 通过 |
| COMP-07 | 对 head 执行 `alembic check` | 输出无待生成迁移 | 同上 | 通过 |
| COMP-08 | 降级到 `20260717_0001` | 项目经历表及两个新增字段被移除 | 同上 | 通过 |
| COMP-09 | 降级后读取旧译员项目 | “旧当前项目”仍保留 | 同上 | 通过 |
| COMP-10 | 降级后执行完整性检查 | `PRAGMA integrity_check` 返回 `ok` | 同上 | 通过 |
| COMP-11 | 再次升级到 head | 新结构恢复，旧译员姓名仍为“保留数据” | 同上 | 通过 |
| COMP-12 | 再次升级后检查完整性 | `PRAGMA integrity_check` 返回 `ok` | 同上 | 通过 |

## 五、开发库副本迁移用例

对象：`backend/app.db` 的临时副本；原库迁移前版本为 `20260717_0001`，共有 29 条译员记录。

| 编号 | 场景与步骤 | 预期结果 | 实测 |
|---|---|---|---|
| DB-01 | 对副本执行 `upgrade head` | 版本为 `20260724_0002` | 通过 |
| DB-02 | 比较升级前后译员数量 | 均为 29 | 通过 |
| DB-03 | 检查自动生成的项目经历 | 0 条 | 通过 |
| DB-04 | 升级后执行完整性检查 | 返回 `ok` | 通过 |
| DB-05 | 对副本降级到 `20260717_0001` | 命令成功 | 通过 |
| DB-06 | 比较降级前后译员数量 | 仍为 29 | 通过 |
| DB-07 | 降级后执行完整性检查 | 返回 `ok` | 通过 |
| DB-08 | 对副本再次执行 `upgrade head` | 版本恢复为 `20260724_0002` | 通过 |
| DB-09 | 再次升级后检查数量和完整性 | 29 条译员，完整性为 `ok` | 通过 |

## 六、基线与安全回归用例

| 编号 | 场景 | 预期结果 | 自动化映射 | 实测 |
|---|---|---|---|---|
| SAFE-01 | 结构完整、无版本号的库执行基线接管 | 业务数据保留并记录版本 | `check_existing_database_baseline` | 通过 |
| SAFE-02 | 未传 `--backup-confirmed` | 拒绝执行并要求备份确认 | `check_baseline_safety_guards` | 通过 |
| SAFE-03 | 严重残缺库尝试接管 | 拒绝接管，不创建版本表 | `check_baseline_safety_guards` | 通过 |
| SAFE-04 | 已知旧库仅缺 `pending_idempotency` | 补齐后通过模型校验 | `check_known_legacy_missing_table_is_repaired` | 通过 |
| SAFE-05 | 待审幂等与并发安全回归 | 不产生重复待审或重复 PO | `test_pending_idempotency.py` | 通过 |

## 七、完整回归用例

| 编号 | 检查项 | 命令或入口 | 结果 |
|---|---|---|---|
| REG-01 | 验收测试默认使用临时服务与临时库 | `test_acceptance_isolation.py` | 通过 |
| REG-02 | 核心业务 HTTP 验收 | `test_acceptance.py` | `71/71` 通过 |
| REG-03 | 请求 Schema 校验 | `test_schema_validation.py` | 通过 |
| REG-04 | 历史数据质量审计 | `test_data_quality.py` | 通过 |
| REG-05 | Docker 启动与迁移前置 | `test_docker_startup.py` | 通过 |
| REG-06 | 前端专项测试 | `frontend/tests/*.mjs` | 全部通过 |
| REG-07 | Agent 载荷与安全请求头 | `translator-mgmt-agent/test_client_payloads.py` | 通过 |
| REG-08 | Python 编译 | `python -m compileall` | 通过 |
| REG-09 | Diff 格式 | `git diff --check` | 通过 |

## 八、首次执行与复验记录

| 测试批次 | 首次执行结果 | 重试数据 |
|---|---|---|
| 迁移专项 | 通过 | 无重试 |
| 开发库副本往返迁移 | 通过 | 无重试 |
| 后端与安全回归 | 通过 | 无重试 |
| 前端、Agent 与静态检查 | 通过 | 无重试 |
| 核心业务验收 | `71/71` 通过 | 无重试 |

## 九、复验命令

```bash
cd "/Users/spellbook/Desktop/Langlobal/译员管理系统"

PYTHONPATH=backend backend/.venv/bin/python backend/tests/test_migrations.py
PYTHONPATH=backend backend/.venv/bin/python backend/tests/test_pending_idempotency.py
PYTHONPATH=backend backend/.venv/bin/python backend/tests/test_schema_validation.py
PYTHONPATH=backend backend/.venv/bin/python backend/tests/test_data_quality.py
PYTHONPATH=backend backend/.venv/bin/python backend/tests/test_docker_startup.py
backend/.venv/bin/python backend/tests/test_acceptance_isolation.py
backend/.venv/bin/python backend/tests/test_acceptance.py

for test_file in frontend/tests/*.mjs; do node "$test_file" || exit 1; done
backend/.venv/bin/python translator-mgmt-agent/test_client_payloads.py
backend/.venv/bin/python -m compileall backend/app backend/tests translator-mgmt-agent
git diff --check
```

开发库副本往返迁移：

```bash
cd "/Users/spellbook/Desktop/Langlobal/译员管理系统"

migration_tmp=$(mktemp -d)
cp backend/app.db "$migration_tmp/app.db"

DB_URL="sqlite:///$migration_tmp/app.db" \
  PYTHONPATH=backend backend/.venv/bin/python \
  -m alembic -c backend/alembic.ini upgrade head
sqlite3 "$migration_tmp/app.db" \
  "SELECT version_num FROM alembic_version; SELECT COUNT(*) FROM translators; PRAGMA integrity_check;"

DB_URL="sqlite:///$migration_tmp/app.db" \
  PYTHONPATH=backend backend/.venv/bin/python \
  -m alembic -c backend/alembic.ini downgrade 20260717_0001
sqlite3 "$migration_tmp/app.db" \
  "SELECT version_num FROM alembic_version; SELECT COUNT(*) FROM translators; PRAGMA integrity_check;"

DB_URL="sqlite:///$migration_tmp/app.db" \
  PYTHONPATH=backend backend/.venv/bin/python \
  -m alembic -c backend/alembic.ini upgrade head
DB_URL="sqlite:///$migration_tmp/app.db" \
  PYTHONPATH=backend backend/.venv/bin/python \
  -m alembic -c backend/alembic.ini check
```

## 十、验收边界

- 本轮只验证本地 SQLite，不代表 PostgreSQL、多实例部署或在线不停机迁移已验收。
- `gender/entity_type` 的 API 读写和枚举校验安排在 07-27，本报告不声明其已完成。
- 旧 `current_project/role` 到项目经历的实际数据迁移安排在 07-28，本报告只验证本次不会提前生成数据。
- Projectlist 导入尚未实现，本报告仅覆盖字段映射文档，不覆盖导入行为。
- 开发库只在副本上执行升降级；原 `backend/app.db` 未执行迁移命令。
