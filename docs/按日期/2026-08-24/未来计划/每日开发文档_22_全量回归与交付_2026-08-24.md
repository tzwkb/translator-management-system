# 每日开发文档 22：全量回归与交付

> 文档状态：未来计划（未实施）  
> 计划日期：2026-08-24（周一）  
> 计划工时：5 小时  
> 时间分配：计划验收 2 小时；缺陷修复和上线缓冲 3 小时

## 一、当日目标

- 在真实数据库副本和真实日报样本上完成最终验收。
- 修复阻塞上线的缺陷。
- 输出迁移、回滚、使用和测试证据。

## 二、计划验收（2h）

### 数据库与迁移

- 备份 SQLite 数据库和上传目录。
- 在副本执行从当前基线到最新版本的全量升级。
- 核对译员、语言对、PO、支付、项目经历和附件数量。
- 执行 `PRAGMA integrity_check`。
- 执行降级可行性检查；涉及数据转换时优先验证备份恢复。
- 执行 Alembic 模型差异检查。

### 业务验收

- 同一译员维护多个当前项目和不同角色。
- 查询过往项目并区分我司/外部。
- 保存一口价和其他价格并生成正确 PO。
- 直接导入原始 Projectlist，重复文件不重复入账。
- 查看跨月累计未付和付款批次。
- 维护五类支付账户，敏感字段加密和脱敏。
- 组合筛选目标译员并导出相同结果。
- 汇总面板统计与明细一致。
- 上传、下载和删除资质附件。

## 三、自动化测试

计划执行：

```bash
backend/.venv/bin/python backend/tests/test_acceptance.py
backend/.venv/bin/python backend/tests/test_migrations.py
backend/.venv/bin/python backend/tests/test_schema_validation.py
backend/.venv/bin/python backend/tests/test_data_quality.py
backend/.venv/bin/python backend/tests/test_pending_idempotency.py
backend/.venv/bin/python backend/tests/test_docker_startup.py
```

同时执行：

- 新增 Projectlist、支付、附件、筛选、档期专项测试；
- `frontend/tests/*.mjs` 全部测试；
- Python 编译检查；
- `git diff --check`；
- Docker 启动和持久化目录检查。

## 四、缺陷缓冲（3h）

缺陷处理顺序：

1. 数据丢失、金额错误、重复导入、敏感信息泄漏；
2. 迁移失败、接口 500、核心流程不可用；
3. 筛选或统计错误；
4. 布局和非阻塞体验问题。

3 小时内未解决的阻塞问题不得降低验收标准，应明确记录根因、影响和剩余工时并顺延发布。

## 五、交付物

- 最终开发交付文档；
- 数据库迁移和回滚说明；
- Projectlist 字段映射及导入操作说明；
- 支付信息字段说明；
- 附件备份和恢复说明；
- 自动化测试报告；
- 资源端 UAT 问题及处理记录；
- 有效每日文档的实际结果补录，以及 2 份国际化撤项记录。

## 六、验收标准

- 基线 `98/98` 及新增测试全部通过。
- 迁移副本数据完整，无未记录模型差异。
- 真实日报抽查金额和数量一致。
- 无 P0/P1 阻塞缺陷。
- 敏感字段和上传文件路径不泄漏。
- 文档中的“计划”与“实际”明确区分。
- 完成的每日文档移入各日期 `已实施` 目录；未完成项保留为未来计划。

## 七、实际交付记录

- 代码版本：
- 迁移版本：
- 测试总数与结果：
- UAT 结果：
- 已知问题：
- 发布/回滚结论：
